{
  "rules": {
    ".read": "now < 1751740200000",
    ".write": "now < 1751740200000",
    "programLeads": {
      "$program": {
        "$leadId": {
          ".read": false,
          ".write": true,
          ".validate": "newData.hasChildren(['program','name','phone','createdAt']) && newData.child('program').isString() && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 120 && newData.child('phone').isString() && newData.child('phone').val().length > 5 && newData.child('phone').val().length <= 20 && (!newData.child('email').exists() || newData.child('email').isString() && newData.child('email').val().length <= 200) && (!newData.child('message').exists() || newData.child('message').isString() && newData.child('message').val().length <= 1000) && newData.child('createdAt').isNumber()"
        }
      }
    },
    "conversations": {
      "$chatId": {
        ".read": "auth != null && (auth.uid != null)",
        ".write": "auth != null && (auth.uid != null)",
        ".validate": "newData.hasChildren(['participants', 'participantDetails'])"
      }
    },
    "messages": {
      "$chatId": {
        ".read": "auth != null && (auth.uid != null)",
        ".write": "auth != null && (auth.uid != null)",
        "$messageId": {
          ".validate": "newData.hasChildren(['senderId', 'receiverId', 'message', 'timestamp']) && newData.child('message').val().length > 0 && newData.child('message').val().length <= 1000"
        }
      }
    },
    "friendshipChats": {
      ".read": true,
      ".write": true,
      "$friendshipId": {
        ".read": true,
        ".write": true,
        "messages": {
          ".read": true,
          ".write": true,
          "$messageId": {
            ".read": true,
            ".write": true,
            ".validate": "newData.hasChildren(['senderId', 'receiverId', 'message', 'timestamp']) && newData.child('message').isString() && newData.child('message').val().length > 0 && newData.child('message').val().length <= 1000 && newData.child('senderId').isString() && newData.child('receiverId').isString() && newData.child('timestamp').isNumber() && (!newData.child('read').exists() || newData.child('read').isBoolean()) && (!newData.child('senderName').exists() || newData.child('senderName').isString()) && (!newData.child('senderImage').exists() || newData.child('senderImage').isString())"
          }
        }
      }
    }
  }
}

